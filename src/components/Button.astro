---
interface Props {
  text: string;
  type: string;
}

const { text, type } = Astro.props;
---

<button
  type={type}
  class="text-white bg-primary focus:outline-none cursor-pointer focus:ring-4 focus:ring-secondary font-medium rounded-full text-sm px-5 py-2.5 text-center"
  >{text}</button
>
